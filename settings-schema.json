{
    "layout": {
        "type": "layout",
        "pages": ["general"],
        "general": {
            "type": "page",
            "title": "General",
            "sections": ["command"]
        },
        "command": {
            "type": "section",
            "title": "CodexBar",
            "keys": ["command-path", "provider", "refresh-interval"]
        }
    },
    "command-path": {
        "type": "entry",
        "default": "/opt/apps/codexbar/codexbar",
        "description": "CodexBar command path"
    },
    "provider": {
        "type": "combobox",
        "default": "both",
        "description": "Provider",
        "options": {
            "Codex + Claude": "both",
            "Codex": "codex",
            "Claude": "claude",
            "OpenAI": "openai",
            "Gemini": "gemini"
        }
    },
    "refresh-interval": {
        "type": "spinbutton",
        "default": 60,
        "min": 15,
        "max": 3600,
        "step": 15,
        "units": "seconds",
        "description": "Refresh interval"
    }
}
